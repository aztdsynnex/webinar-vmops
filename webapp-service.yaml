apiVersion: v1
kind: Service
metadata:
  name: webapp-service
  namespace: webinar-vmops
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  selector:
    app: webinar-vm
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
