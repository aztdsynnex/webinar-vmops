apiVersion: v1
kind: Service
metadata:
  name: vm-ops-svc
  namespace: vm-ops
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  selector:
    app: vm-ops
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
